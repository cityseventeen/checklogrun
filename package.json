{
    "name": "checklogrun",
    "version": "0.2.1",
    "description": "The library promotes the separation of responsibilities, allowing the logic, valitadion and logging to be kept in distinct modules, organizing the code into separate files and reducing the complexity of the main funciton",
    "main": "src/main.js",
    "scripts": {
        "test": "mocha --exit && npm version patch && npm run git",
        "git": "set /p message=\"write commit message\" && git add . && call git commit -m \"%message%\""
    },
    "type": "module",
    "author": "Raffaele Balsamo",
    "license": "SEE LICENSE IN <LICENSE>",
    "devDependencies": {
        "@types/chai": "^5.0.0",
        "@types/mocha": "^10.0.8",
        "@types/node": "^22.7.4",
        "chai": "^5.1.1",
        "mocha": "^10.7.3",
        "sinon": "^19.0.2"
    },
    "dependencies": {
        "enum": "^3.0.4",
        "laziest-error": "github:cityseventeen/laziesterror"
    },
    "config": {}
}
